# STATE (updated 2026-07-16 22:15) — history: git log + docs/STATE-ARCHIVE.md

## Done
- CRON_SECRET repo secret set (Rishet11/reviewos) + 15-min cron VERIFIED (manual workflow_dispatch run 29521166128 = success; direct curl dispatch = 200, wrong secret = 401). Note: `gh secret set --body -` stores literal "-", use stdin pipe without --body.
- FIXED "200" embedded-admin bug (commit 4368f96, deployed live on Render): billing gate in app/routes/app.tsx threw a plain react-router `redirect` from `billing.require` onFailure, dropping embedded host/id_token params so the iframe bounce rendered a bare status code. Now uses the App-Bridge-aware `redirect` from `authenticate.admin`; added a root ErrorBoundary (via Layout) as a safety net. Verified: embedded admin renders the Billing page ("Active plan: Pro") instead of "200". typecheck+lint clean, diff-reviewer passed.
- Verification sweep: Phase 5/6 claims confirmed; graphify current; fixed 5 TS errors (widget mount.ts) + added marketplace test (93/93); docs drift fixed; all Phase 6 work committed+pushed (c3055b9..90999db).
- Prisma migration baseline: replay on scratch Postgres == schema.prisma, migrate status clean (6 migrations).
- Phase 8 partial: Render service LIVE https://reviewos-6p4p.onrender.com (free tier, Docker, rootDir shopify-app, env vars set via API, service id srv-d9cf1ht7vvec73cbsju0). Separate prod app "ReviewOS" (client_id 4d89e9f395a278d509a5528c42d7a740, shopify.app.production.toml); version reviewos-2 released WITHOUT orders/* webhooks (blocked on protected-customer-data). Prod OAuth+install VERIFIED; dispatch route VERIFIED (401/200); app proxy VERIFIED live (200 JSON via storefront; binds at install time, required reinstall). GH Actions cron workflow committed (secret NOT set). docs/LISTING.md (App Store copy) + docs/lighthouse-7e.md (baseline 92.41 weighted: home 89/product 90/collection 96; floor 82.41; runner scripts/lighthouse-7e.mjs).

## In progress
- PCD filing: form found (Partners → ReviewOS → API access requests → Protected customer data → Request access). Step 1 done in-form: data use = "Protected customer data" + "Email" field only (code reads only order email + customer ID; name is reviewer-entered), reasons = App functionality + Marketing or advertising. Step 2 (data protection MCQs) answered truthfully; honest No's on #2/#4/#8/#16 flip to Yes once policy docs are published, plus permanent gaps #12 DLP + #15 access-logging. Dev-store data access granted by Step 1 alone (submission-for-review deferred to App Store submission).
- Policy docs drafted + verified truthful (uncommitted): docs/PRIVACY.md, docs/DATA-RETENTION.md, docs/SECURITY.md. FOLLOW-UPS before submission: (a) real privacy contact inbox (placeholder privacy@reviewos.app), (b) public /privacy URL (repo doc != merchant-visible), (c) build DLP + PII access logging if pursuing those attestations.

## Next
1. Protected-customer-data (VERIFIED via docs 2026-07-16): requested in PARTNER dashboard (partners.shopify.com → Apps → app → "API access requests" → Protected customer data access → Request access), NOT the dev dashboard (new dev dashboard has no such panel; confirmed dev-dashboard apps may not appear in Partners = known Shopify bug). NOT needed for dev-store testing (read_orders + order webhooks work on owner's dev store); REQUIRED before App Store submission (Shopify reviews it = long pole, file early). Level 2 PII reqs (encrypt at rest+transit+backup, retention limits, DLP, access logs, incident response) + compliance webhooks (already in toml). After approval: re-add 3 orders/* webhooks to shopify.app.production.toml + user runs `shopify app deploy --config production --allow-updates`. First check: does ReviewOS appear in partners.shopify.com/Apps?
3. Re-add theme blocks in theme editor, then "after" Lighthouse pass (same runner; must stay ≥82.41).
4. R2 + Resend creds into Render env; live E2E (media, verified-buyer order, review-request email).
5. Billing test charge (dev app) + real-mode check (prod). 6. Render Starter upgrade + rotate Neon/Groq/RENDER_API_KEY before submission. 7. Listing assets per docs/LISTING.md.

## Gotchas
- Dev: `cd shopify-app && shopify app dev` (dev app reinstalls and COLLIDES with prod on /apps/reviewos proxy; uninstall one). Prod deploys: user runs deploy command via `!` (permission-gated for Claude).
- Free tier cold starts: proxy 503s + webhook 500 blips until Starter upgrade. Shopify retries webhooks; proxy just fails.
- Env names: DATABASE_URL, GROQ_API_KEY, SHOPIFY_API_KEY/SECRET, SHOPIFY_APP_URL, CRON_SECRET, REVIEW_VERIFICATION_SECRET, SENTRY_DSN (empty=off), R2_*, RESEND_*, RENDER_API_KEY (in shopify-app/.env, exposed in chat, rotate after Phase 8).
- Storefront password cookie is `_shopify_essential` (not storefront_digest); store pw imefru.
