# STATE (updated 2026-07-16) — full history: docs/STATE-ARCHIVE.md

**Phase 6 (media & collection) is fully built AND migrated** — all 3 slices (A: media upload, B: order linkage/verified-buyer, C: review-request email) landed, all 3 migrations applied to the live shopify-app Neon DB, full suite green (11 files, 91/91 tests, tsc clean, `shopify app build` clean). What's left is external creds (R2, Resend, CRON_SECRET) and live end-to-end verification on the dev store — no more code to write for this phase.

## Done
- **Verification sweep (2026-07-16, 3 parallel subagents)**: independently re-verified Phase 5 Slice A (all 11 claims confirmed with file:line evidence) and Phase 6's riskiest claims (ContentLength-signed presign, PendingReviewMedia ownership gate, product_id-only GID derivation, dispatch sentCount/failedAttempts separation, GDPR FK ordering checked against actual migration SQL, timingSafeEqual) — all confirmed. graphify-out checked: current, no refresh needed. TWO findings, both fixed same session: (1) `reviewos` typecheck was FAILING with 5 TS errors in `widget/src/shopify/mount.ts` (un-narrowed `productId` in async closures; the earlier "tsc clean both apps" claim was false for the working tree) — fixed via `const pid: string = productId` after the guard, bundle rebuilt into extension assets; (2) `getMarketplaceStats` (the proxy-facing read path) had zero test coverage — 2 tests added to `marketplace.test.ts` (shop-scoping + unknown-slug short-circuit). Post-fix: reviewos typecheck clean, shopify-app tsc clean, vitest **93/93 (11 files)**, `shopify app build` clean. Docs drift also corrected: ROADMAP.md DONE markers added (phases 2/3/6/7a-c), stale Phase 8 "Postgres migration" line replaced, PRD.md false "5b connector code is built" claim corrected (5b was never built). **Decisions taken (user-approved): next work = submission path (Phase 8), 7d deferred to post-submission; prod host = Render; Phase 6 work committed + pushed as atomic commits.**
- **Phase 6 Slice C (post-purchase review-request email, shopify-app only)**: fires after `orders/fulfilled`, deep-links back to the write-review flow with Slice B's existing signed token, no queue infra (deferred to Phase 8). Schema: new `ReviewRequest` (shop-scoped, `@@unique([shop, shopifyOrderId, productId])`, FK to `Product` ON DELETE RESTRICT) + `EmailSuppression` (`@@unique([shop, email])`) tables. `app/services/review-requests.server.ts`: `createReviewRequestsForOrder` — guards on missing email / `EmailSuppression` hit, resolves local `Product` rows by `shopifyProductId` (skips unsynced line items, no dangling `productId`), cohort (`first_time`/`repeat`) from whether any prior `ReviewRequest` exists for that email, delay-days read from `Settings` (`reviewRequestDelayDaysFirstTime`/`reviewRequestDelayDaysRepeat`, defaults 5/3, parse-failure falls back to default), upserts on the compound unique so webhook redelivery is idempotent (`created` count only counts genuinely new rows, not redelivery no-ops — a diff-review pass caught the first version over-counting). `app/services/review-requests-dispatch.server.ts`: `findDueReviewRequests` (pending + due + `sentCount < 3`), `dispatchReviewRequest` (suppression check → `suppressed`; conversion check via existing `Review` → `converted`; builds the deep link with Slice B's `signVerificationToken` — no second token scheme; success → `sentCount+=1`, reschedule +7d, `exhausted` at 3; transport failure → `failedAttempts+=1` (capped at 5 → terminal `failed`), never touches `sentCount` so a Resend outage can't burn the customer's 3-touch allowance), `dispatchDueReviewRequests` (trigger-agnostic on purpose — swap-in point for Phase 8's queue processor). Exposed via secret-guarded `app/routes/api.review-requests.dispatch.tsx` (constant-time `CRON_SECRET` comparison via `crypto.timingSafeEqual`; prod trigger = Render Cron Job every ~15 min hitting this route, dev = manual curl). Resend: `npm install resend`, `app/services/email/resend.server.ts` (thin wrapper, `List-Unsubscribe` header), `app/services/email/templates/review-request.ts` (pure function, cohort-varied subject/HTML/text, no `<style>` blocks). Unsubscribe: `app/lib/unsubscribe-token.server.ts` (deliberately separate, simpler one-line HMAC over `shop:email` — not the same scheme as `signVerificationToken`, different purpose) + GET `app/routes/api.review-requests.unsubscribe.tsx` (verifies, upserts `EmailSuppression`, returns a confirmation page). Both TODO seams from Slice B wired for real: `webhooks.orders.fulfilled.tsx` now calls `createReviewRequestsForOrder` (try/catch, `customerName: null` since `OrderCapture` has no name field) and `proxy.reviews.tsx` now marks the matching `ReviewRequest` `converted` with `completedReviewId` on a verified submission (resolves `OrderCapture.shopifyOrderId` from `orderMatch.orderCaptureId` first). GDPR (`gdpr.server.ts`): `redactShop` deletes `reviewRequest` **before** `product` in the same `$transaction` array (FK-order bug caught by diff-review pre-fix — deleting `product` first would have thrown `ON DELETE RESTRICT` and rolled back the whole shop erasure) + deletes `emailSuppression`; `redactCustomer` suppresses (`reason: "customer_redact"`) **before** deleting matching `ReviewRequest` rows outright (no anonymized remnant kept — it's an operational record, not a review; suppress-first ordering means a later order webhook for the same redacted email can't recreate rows); `collectCustomerData` extended with `reviewRequests`. Env: `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `CRON_SECRET` added as placeholders to both `.env` and `.env.example`. VERIFIED: tsc clean, `shopify app build` clean, vitest 91/91 (up from 90 across 11 files, +21 new tests: `review-requests.test.ts` 11, `review-requests-dispatch.test.ts` 9, `gdpr.test.ts` extended). diff-reviewer pass caught 1 real bug (the FK-order rollback risk above, fixed) + 2 minor issues (non-constant-time `CRON_SECRET` compare, misleading `created` counter — both fixed), confirmed no scope creep and all "not implemented" TODO seams wired correctly.
  - **Migration APPLIED (2026-07-16)**: `20260716140000_review_requests_email` run via psql + `prisma migrate resolve --applied` on the live shopify-app Neon DB. Full suite re-verified after: 11 files, 91/91 tests, tsc clean, `shopify app build` clean.
  - **Still needs you**: real `RESEND_API_KEY` + a verified `RESEND_FROM_EMAIL` sending domain in Resend, and a real random `CRON_SECRET` value (currently empty placeholders in `.env`) before any email can actually send. Then either wire a Render Cron Job (`POST /api/review-requests/dispatch`, header `x-cron-secret: $CRON_SECRET`, ~15 min interval) or trigger manually via curl in dev.
  - **Not built (explicitly out of scope this slice)**: Resend bounce/complaint webhook receiver — `EmailSuppression.reason` already has room for a future `"bounce"` value with no migration needed, wire it when it's actually needed.
  - Live end-to-end test (real fulfilled order → `ReviewRequest` created → dispatch route sends → click deep link → submit review → row flips to `converted`) not done this session — needs the migration applied + Resend creds + a live store first.
- **Phase 6 Slice B (verified-buyer / order linkage, shopify-app only — no demo-app portability concern, order data only exists in Shopify)**: webhook-driven, never queries historical orders live. `shopify.app.toml` gained `read_orders` scope + 3 webhook subscriptions (`orders/paid` → `webhooks.orders.paid.tsx`, `orders/fulfilled` → `webhooks.orders.fulfilled.tsx`, `orders/cancelled` → `webhooks.orders.cancelled.tsx`), all via `authenticate.webhook` (declarative registration on `shopify app deploy`, no manual `registerWebhooks()` call needed). Schema: new `OrderCapture` (shop-scoped, `@@unique([shop, shopifyOrderId])`) + `OrderLineItem` (child, FK to OrderCapture) tables; `Review.verifiedOrderId String?` added as a deliberate soft reference (no `@relation`/FK — matches the `MarketplaceStat.url`-as-plain-string convention, so OrderCapture rows can be pruned/GDPR-redacted independently). `app/services/order-capture.server.ts`: `captureOrder` (upserts on shop+orderGID, idempotently replaces line items via deleteMany+createMany — safe against webhook redelivery), `markOrderFulfilled` (returns the row with lineItems for the Slice C handoff), `cancelOrderCapture` (sets `cancelledAt`, then revokes `verifiedBuyer` on every `Review` whose `verifiedOrderId` points at it — the only revocation path; partial refunds deliberately NOT handled, documented scope boundary). `app/services/order-verification.server.ts`: the ONE signed-token scheme for this phase (`signVerificationToken`/`verifyVerificationToken`, HMAC-SHA256 + `timingSafeEqual`, 90-day default TTL; known gap documented — not single-use, replay-able) plus `matchOrderForReview` (token path first — works even for a product not yet synced locally, since the token carries `shopifyProductId` independently; falls back to email+local-product-GID `findFirst` match, `cancelledAt: null` excluded from both paths). Wired into `app/routes/proxy.reviews.tsx` (new `verificationToken` body field; `matchOrderForReview` called right after `resolveProductForShop`; result's `verified`/`orderCaptureId` passed into `createReview` as `verifiedBuyer`/`verifiedOrderId`) and `app/services/reviews.server.ts` (`CreateReviewInput` extended with both fields, defaulting to `false`/`null` so Slice A's media-only submissions and admin-created reviews are unaffected). GDPR (`gdpr.server.ts`): `redactShop` now also deletes `orderLineItem`→`orderCapture` (child-first, in the same `$transaction`); `redactCustomer` nulls `OrderCapture.customerEmail`/`customerId` (keeps the row/line-items so revocation logic still works post-redaction); `collectCustomerData` return shape changed from a bare `Review[]` to `{ reviews, orders }` (orders = matching `OrderCapture` rows with `lineItems`), still null-email-guarded. Two TODO-comment seams left for Slice C (review-request emails, not built yet, module doesn't exist): in `webhooks.orders.fulfilled.tsx` (call `createReviewRequestsForOrder` once `review-requests.server.ts` exists) and in `proxy.reviews.tsx` (mark a `ReviewRequest` `converted` on a verified submission) — deliberately comments, not imports, since importing a nonexistent module would break the build. **diff-reviewer pass caught 1 real bug**: the original design's line-item normalization (`li.admin_graphql_api_id ?? gid-from-product_id`) would have stored a **LineItem GID** instead of a **Product GID** in `OrderLineItem.shopifyProductId` for virtually every real Shopify order webhook (`admin_graphql_api_id` on a line item is always the line item's own GID, not the product's — `product_id` is the only reliable field), which would have silently broken both matching paths against real orders. Fixed: `shopifyProductId` is now derived from `product_id` only; regression test added proving a present `admin_graphql_api_id` on a line item is ignored. VERIFIED: tsc clean, `shopify app build` clean, vitest 68/71 passing (3 skipped — same class of pre-existing-migration gap as Slice A, see below, not a logic bug: confirmed by error text `column verifiedOrderId does not exist`). 19 new tests added (`order-capture.test.ts` 8, `order-verification.test.ts` 11), `gdpr.test.ts` updated for the new tables/shape.
  - **Migration APPLIED (2026-07-16)**: `20260716120000_review_order_capture` run via psql + `prisma migrate resolve --applied` on the live shopify-app Neon DB. All 3 previously-skipped tests in `reviews.test.ts` now pass.
  - **Production gate, not started this session**: this whole feature needs Shopify's protected-customer-data approval before it can read real customer emails/order data in production (`order.customer.email`, the `email`/`customer.email` fields this service reads from webhook payloads) — filing is in progress in parallel, tracked as a Next item below. Fine to build/test against the dev store now.
- **Phase 6 Slice A (media upload, photo/video on reviews)**: resumed + finished after an interrupted attempt left most of it built (see below). Prisma `ReviewMedia` gained `storageKey`/`mimeType`/`sizeBytes`/`thumbnailUrl` (nullable, unpopulated, reserved for future video-thumbnail work) in both schemas; shopify-app only also gained `PendingReviewMedia` (shop-scoped orphan tracker: a presigned upload lands here, gets deleted once a review claims it). `app/services/media.server.ts` (duplicated across both apps, kept in sync manually): `MAX_IMAGE_BYTES`(10MB)/`MAX_VIDEO_BYTES`(200MB)/`MAX_MEDIA_PER_REVIEW`(5), `classifyContentType`, `validateMediaUpload` — pure, unit tested (`app/services/media.test.ts`, 10 tests). shopify-app: `app/lib/r2.server.ts` (Cloudflare R2 via `@aws-sdk/client-s3` + presigner; `presignReviewMediaUpload` signs `ContentLength` into the PUT so a client can't declare a small size then stream a bigger file; `deleteReviewMediaObject`), `app/routes/proxy.media.presign.tsx` (HMAC via `requireProxy`, validates, records `PendingReviewMedia`). Demo app: `app/routes/api.media.presign.ts` + `api.media.upload.$key.ts` (local-disk stand-in, same response contract, path-traversal guarded, independently caps actual bytes written via a counting Transform stream regardless of what `Content-Length` claims). `createReview` (both `reviews.server.ts`) wraps review + `ReviewMedia.createMany` in one `$transaction`, caps media server-side; shopify-app version also requires every claimed `storageKey` to match a live `PendingReviewMedia` row for that shop before accepting it (closes an ownership gap a review-pass caught: without this, any public R2 URL — another shop's, or an already-claimed one — could be attached to a review with no upload). `proxy.reviews.tsx` / demo `api.reviews.ts` validate incoming `media[]` (`type` ∈ image/video, `storageKey` present, URL under the expected base — shopify-app compares parsed URL **origin**, not a raw string prefix, after a review pass caught `startsWith` being spoofable via a suffixed domain). Widget: new `reviewos/widget/src/media-upload.ts` (`uploadReviewMedia` sequential upload-then-attach; object-URL preview cache via `WeakMap` + explicit `revokePreviewUrl`/`revokeAllPreviewUrls`, wired at modal-reopen/remove-file/success in both `widget.ts` and `shopify/mount.ts` — the interrupted attempt had built the cache but never called revoke, a real leak, now fixed). `blocks/write.ts` file input (max 5, disabled during upload to close a re-render race a review pass caught), `blocks/feed.ts` branches `<video>` vs `<img>` by type; `ugc-gallery.ts` deliberately untouched (images-only gallery is correct as-is). Admin `app.reviews.tsx` `ReviewRowCard` shows a 64x64 thumbnail strip. `scripts/sweep-media.ts` + `cleanup-orphaned-media.server.ts` (`sweepOrphanedMedia`, 24h cutoff, per-row try/catch) + `npm run cleanup:media`. VERIFIED: tsc clean both apps, `shopify app build` clean, widget build ok, vitest 49/52 passing (3 pre-existing skips) + 1 suite (`reviews.test.ts` cross-shop isolation) failing **only** because the migration below isn't applied yet — confirmed by error text (`ReviewMedia.storageKey does not exist`), not a logic bug. diff-reviewer pass caught 5 real issues (URL-origin spoofing, media-ownership gap, missing `ContentLength` binding, unbounded demo disk write, remove-during-upload race) — all fixed above, re-verified clean after.
  - **Both migrations APPLIED (2026-07-16)**: demo Neon DB (`manual-migration-review-media-r2-fields.sql`, 62 rows backfilled) and shopify-app Neon DB (`20260715172013_review_media_r2_fields`, 121 rows backfilled + `prisma migrate resolve --applied`). Full shopify-app suite re-verified: 7 files/52 tests → after Slices B+C landed, 11 files/91 tests, all green. R2 credentials (`R2_ACCOUNT_ID` etc, placeholders already in `shopify-app/.env`/`.env.example`) still needed from you before any live upload can actually work.
- Phase 5 Slice A (marketplace trust badges, merchant-owned data): manual marketplace stats → storefront trust badges. `app/services/marketplace.server.ts` gained shop-scoped CRUD: `listSources`/`upsertSource`/`deleteSource` (deleteMany shop-scoped), `upsertStat` (validates sourceId∈shop via findFirst before write; resolves product via `resolveProductForShop`), `deleteStat`; `getMarketplaceStats` untouched. Manual stat = authoritative badge number (merchant types the public figure, e.g. "Amazon 4.6 · 12,431"; no recompute). New admin `app/routes/app.marketplace.tsx` (sources + stats cards, `s-number-field` for numerics, logo opt-in w/ trademark note, try/catch → {ok,error} toast) + nav link in `app.tsx`. New proxy `app/routes/proxy.marketplace.tsx` (GET ?product=slug → `{stats: MarketplaceStat[]}`, requireProxy HMAC, DB-only no Groq). Widget: `fetchMarketplaceStats` in `reviewos/widget/src/shopify/api.ts`, `trust-badges` block wired in `mount.ts` (reuses existing `renderTrustBadges`), new `extensions/reviewos-widgets/blocks/trust-badges.liquid` (schema name 22 chars), bundle rebuilt. Tests `app/services/marketplace.test.ts` (shop-scoping + cross-shop sourceId write rejected). Seed: self-contained `seedTrustBadgeDemo()` (Amazon source + stat on the-collection-snowboard-liquid under reviewos.myshopify.com). VERIFIED: tsc clean, vitest 42/42, `shopify app build` clean, widget build ok, diff-review clean. Live theme-editor visual check outstanding (needs browser). **Decision: marketplace ratings stay SEPARATE — never blended into standard `reviews.rating`/overall star badge (would inflate "Verified by Shop" dishonestly).**
- **GATE finding (web-researched 2026-07-15): no mainstream marketplace exposes review BODIES to sellers.** Amazon SP-API confirmed "not available" (Dec 2024); Flipkart Seller API has zero review endpoints; Nykaa unverifiable. Legit merchant-owned review-body feedstock exists ONLY via review platforms (Trustpilot Business API, Google Business Profile API, Judge.me/Loox/Yotpo CSV export). ⇒ Slice B (marketplace CSV review import + per-marketplace AI summaries) has no legitimate marketplace data source; a "marketplace review CSV" could only come from scraping — same underlying provenance as the Phase 5b live-fetch route, just with liability shifted to the merchant. **Slice B parked in Phase 7d AS-IS (not rescoped) per user.** Marketplace aggregation ships at Slice A.
- Phase 7c (metafield writeback): `app/services/metafields.server.ts` — `computeRatingAggregate` (shop+productId+approved), `syncRatingMetafields` (shop-scoped `findFirst`; writes standard `reviews.rating` type `rating` JSON `{value,scale_min:"1",scale_max:"5"}` + `reviews.rating_count` type `number_integer` via one `metafieldsSet` to product GID; skips GID-less), `backfillAllRatingMetafields` (returns `{synced,skipped,failed}`, logs userErrors as failed not synced). Wired: `app.reviews.tsx` moderate action syncs (try/catch, won't 500), `app.products.tsx` "Backfill product ratings" button (toast shows synced/skipped/failed). VERIFIED: tsc clean, vitest 36/36 (8 metafields), build clean, diff-review (2 real bugs found + fixed: unscoped product lookup, userErrors counted as success). 4 atomic commits, pushed.
- Phase 7 (App Store blockers): GDPR compliance webhooks + billing. VERIFIED: tsc clean, vitest 28/28, `shopify app build` clean, diff-review pass (1 real bug found + fixed). Landed as 7 atomic commits; repo now on GitHub (github.com/Rishet11/reviewos).
  - **GDPR (7b)**: `app/services/gdpr.server.ts` — `redactShop(shop)` deletes all rows across every model in one `prisma.$transaction` (child-first FK order: reviewMedia→review→aiSummary→marketplaceStat→marketplaceSource→attributeDefinition→product→settings→session; every where shop-scoped, no cross-shop leak). `redactCustomer(shop,{email})` anonymizes identity (customerName→"Anonymous", customerEmail→null) + deletes their media; null-email guard (name-only reviews unlinkable, documented gap). `collectCustomerData(shop,{email})` shop+email-scoped read; null-email returns [] (guard added after diff-review caught it would else leak whole shop). Single route `app/routes/webhooks.compliance.tsx` switches on topic (CUSTOMERS_DATA_REQUEST/CUSTOMERS_REDACT/SHOP_REDACT — verified exact strings from `@shopify/shopify-api` privacyTopics); never touches `session` (safe post-uninstall); always 200. Topics in `shopify.app.toml` as one `compliance_topics` subscription → `/webhooks/compliance`. Tests mock PrismaClient (never hit live Neon DB, LEARNINGS #14): 5 tests, prove shop-scoping + null-email guards. data_request result is intentionally discarded in the route (merchant fulfills delivery; Shopify requires only receipt).
  - **Billing (7a)**: `app/shopify.server.ts` — one `Pro` plan (\$9.99 USD/30-day, 14-day trial, `BillingReplacementBehavior.ApplyImmediately`). NO \$0 Free plan (\$0 recurring 422s; trial = free experience). Exports `BILLING_TEST = NODE_ENV !== "production"` and `BILLING_PLANS = ["Pro"]`. `app/routes/app.tsx` loader gates whole app via `billing.require({plans:[...BILLING_PLANS], isTest:BILLING_TEST, onFailure:→redirect("/app/billing")})`, exempting `/app/billing` (no redirect loop; verified against lib source). During trial `require` passes → app usable free. New `app/routes/app.billing.tsx` (Polaris web components): loader `billing.check` (returns boolean in this mode, no returnObject), action `billing.request` (App-Bridge iframe-aware). Nav link added.
  - **AWAITING USER**: run the test charge. `cd shopify-app && shopify app dev` → open app (trial active) → Billing → Subscribe → CONFIRM Shopify approval screen loads FULL-PAGE not blank iframe → approve test charge (isTest, no real money) → confirm redirect to /app?charge=success + active sub. Billing auto-uses real charges in prod build (BILLING_TEST false).

- Phases D, 0, 1, 2 (see archive). Phase 3a: multi-tenancy (`shop` on all domain models, shop-scoped uniques, services take `shop` first, migration 20260715120000, cross-shop isolation tests).
- Phase 3b: signed App Proxy (`app/lib/proxy-verify.server.ts`; endpoints /reviews /distribution /attributes /summary /status) + theme extension `extensions/reviewos-widgets` (blocks: review-feed, star-badge, rating-distribution, write-review, filter-chips; filter sync via URL + `reviewos:filters-changed` event). LIVE-VERIFIED by user in theme editor on reviewos.myshopify.com.
- Phase 4: real product linkage + AI summaries wired in.
  - `app/services/products.server.ts`: `syncProductsFromCatalog(shop, admin)` (paginates Admin GraphQL `products(first:250)`, upserts on `shop_slug`, slug=handle, stores GID) + `resolveProductForShop(shop, handle)` (findUnique → enrich via `unauthenticated.admin(shop)` `products(query:"handle:..")` → bare-row fallback; P2002 race-guarded; `unauthenticated` lazy-imported so proxy import graph doesn't instantiate shopifyApp).
  - Schema: added `Product.shopifyProductId String?` (GID; enables future reviews.rating metafield writeback). Applied via `prisma db push`.
  - Proxy routes (`proxy.reviews` action, `proxy.summary`, `proxy.attributes`) resolve via `resolveProductForShop` (auto-create on unknown handle). `proxy.reviews` loader + `proxy.distribution` degrade to empty (no create) on miss by design.
  - Admin: new `app/routes/app.products.tsx` (lists products + "Sync from Shopify catalog") + nav link in `app.tsx`; `app.reviews.tsx` gained `regenerate-summary` intent → `getOrGenerateSummary(shop, productId, "overall", {}, force:true)`.
  - Widget: `ai-summary.liquid` block + `renderAiSummary` wired into `shopify/mount.ts` (own `aiSummarySeq`, refetches on `reviewos:filters-changed`, hides when null); `fetchSummary` forwards attr filters for cohort summaries. Bundle rebuilt into extension assets via `reviewos/widget/build.mjs`.
  - VERIFIED: typecheck clean, vitest 23/23, `shopify app build` clean (theme-check ok), widget build ok, diff-review clean.
  - LIVE-DEBUGGED (2026-07-15): AI summary block showed nothing on a real store product because (a) `getAiProvider()` used `?? "groq"` but `.env` has `AI_PROVIDER=` (empty) → selected provider `""` → threw on every generation. Fixed to `|| "groq"` (`app/services/ai/index.ts`). (b) Real store products had 0 reviews (seed reviews only on fictional slugs). Additively seeded 16 approved reviews + `ridingStyle`/`skillLevel` attribute defs on `the-collection-snowboard-liquid`. Groq then generated overall (16 reviews) + `ridingStyle=powder` cohort (3) summaries, both cached, verified via the real `getOrGenerateSummary` (tsx). Sync confirmed: 16 real store products carry GIDs. User to refresh preview + restart `shopify app dev` so the AI_PROVIDER fix loads for uncached cohorts.
  - RE-VERIFIED LIVE (2026-07-15, 2nd pass): started `shopify app dev`, called `getOrGenerateSummary` directly against live Neon DB for `the-collection-snowboard-liquid` — overall summary (16 reviews) and `ridingStyle=powder` cohort summary (3 reviews) both generated correctly via Groq with distinct pros/cons/summaryText, confirming exit criterion "summary changes when filter chips change." App Proxy HTTP layer itself untested this pass (its HMAC secret is injected at runtime by Shopify CLI, not present in `.env`, so it can't be forged from curl outside a real storefront request) — proxy signature verification remains covered only by the existing unit tests, not a live end-to-end request.

## Decisions
- **AI: Groq only.** No Anthropic. Provider registry (`app/services/ai/index.ts`) stays pluggable but only `groq` is registered; do not add Anthropic. Remove/ignore the empty `AI_PROVIDER=` line in `.env` (or set `AI_PROVIDER=groq`).
- Docs: `docs/FOR-STAKEHOLDER.md`; RESEARCH.md 2026-07-15 findings. graphify knowledge graph refreshed (`graphify-out/`).
- Phase 6 + verification-sweep fixes committed and pushed as atomic commits (2026-07-16, user-approved this session; see git log).

## In progress
- None. Phase 6 (all 3 slices) code-complete and migrated. Nothing left to build — remaining items are external creds + live verification (below).

## Next
1. Get real Cloudflare R2 credentials into `shopify-app/.env` (`R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_BASE_URL`) — currently empty placeholders, uploads can't work without them. Then live-test the photo/video upload flow in a browser.
2. Get real `RESEND_API_KEY` + a verified `RESEND_FROM_EMAIL` sending domain, and a real random `CRON_SECRET` value. Then wire a Render Cron Job (`POST /api/review-requests/dispatch`, header `x-cron-secret: $CRON_SECRET`, ~15 min interval) or trigger manually via curl in dev.
3. Live-test Slice B end-to-end on the dev store: place a real test order, confirm `OrderCapture`/`OrderLineItem` populate from the `orders/paid`/`orders/fulfilled` webhooks, submit a review with the matching email/product, confirm `verifiedBuyer: true`.
4. Live-test Slice C end-to-end: fulfill a test order → confirm `ReviewRequest` rows created with the right `scheduledSendAt` → curl the dispatch route → confirm a real Resend email arrives with a working deep link → submit that review → confirm the `ReviewRequest` flips to `converted`.
5. Visually confirm in theme editor (needs browser): (a) AI-summary block renders, filter click swaps cohort summary, hides when < 3 reviews; (b) Phase 5 Slice A trust-badges block renders the manual marketplace stat with working link-out on a real product; (c) Phase 6 media gallery/thumbnails render correctly.
6. Run the Phase 7 billing test charge (steps in Done above) — Shopify-hosted approval screen, requires a real browser/merchant login; cannot be scripted or curled. Last unverified Phase 7 item.
7. Get stakeholder answers to docs/FOR-STAKEHOLDER.md (Phase 5 scoping now resolved; still needed for later phases).
8. Rotate exposed Neon DB password + GROQ_API_KEY (pasted in chat earlier session).
9. File Shopify protected-customer-data access request (in progress in parallel per user) — needed before Phase 6 Slice B/C can read real customer emails/order data in production; dev-store testing is unaffected.
10. ~~Prisma migration-history drift~~ RESOLVED (2026-07-16): backdated baseline migration `20260715150000_product_shopify_gid` created + `migrate resolve --applied` on live Neon DB. VERIFIED both ways: `migrate status` clean (6 migrations), and full history replayed on a scratch local Postgres reproduces schema.prisma exactly (`migrate diff` = no difference). Future schema changes can use normal `prisma migrate dev` again.
11. **Phase 8 items previously untracked here (host decision made: Render)**: (a) Render web service for shopify-app + Render Cron Job for the dispatch route (do secret rotation #8 FIRST, rotated values go straight into Render env); (b) error monitoring (Sentry free tier or Render alerts); (c) Phase 7e Lighthouse pass (blocks with/without on dev store, exit ≤10 points); (d) App Store listing assets [USER]: icon, screenshots, listing copy, support contact, privacy policy URL. Redis/BullMQ deliberately NOT provisioned for submission — cron dispatch is the designed seam.
12. Post-submission (deferred by decision): Phase 7d (Judge.me/Loox CSV import — the switching-cost feature, first post-submission build), Phase 5b (post-approval only). Goal-risk note from truth-mode review: distribution, not code, is the bottleneck — line up 3-5 pilot merchants (India D2C niche per PRD) in parallel with Phase 8.

## Known tradeoff (Phase 4)
- GET proxy loaders (`/summary`, `/attributes`) can auto-create a bare Product row for a typo'd/crawled handle before a merchant ever syncs. Low harm (empty shop-scoped rows, wiped on reseed); accepted for now. Revisit if crawler noise pollutes the catalog.

## Gotchas
- Run: `cd shopify-app && shopify app dev` (dev store reviewos.myshopify.com); kill stale port first.
- Env names: DATABASE_URL, GROQ_API_KEY, SHOPIFY_API_SECRET, SHOP_DOMAIN. ROTATE Neon password + GROQ_API_KEY (exposed in chat). Protected-customer-data request not started.
- Never put `.test.ts` under `app/routes/` — React Router loads it as a route; vitest import 500s every request (bit us live).
- Neon P1001 at dev start = scale-to-zero cold-start blip; retry or add `connect_timeout=15` to DATABASE_URL.
- Reseed with `SHOP_DOMAIN=reviewos.myshopify.com npm run seed`; block `{% schema %}` name max 25 chars.
